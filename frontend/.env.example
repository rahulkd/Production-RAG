# Base URL of the FastAPI RAG service. Server-side only — never exposed to the browser.
# Local dev (API on the host):     http://localhost:8000
# Docker Compose (API on the net): http://api:8000
RAG_API_URL=http://localhost:8000
